// Real credentials live in the gitignored credentials.local.ts, created from
// credentials.local.example.ts at install time — see that template for setup.
import { LOCAL_APP_ID, LOCAL_APP_KEY } from './credentials.local';

export const DEFAULT_APP_ID = LOCAL_APP_ID;
export const DEFAULT_APP_KEY = LOCAL_APP_KEY;
