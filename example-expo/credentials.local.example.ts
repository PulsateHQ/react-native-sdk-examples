/**
 * Template for `credentials.local.ts`, which is gitignored. `npm install`
 * creates that file from this one; open it and paste the 64-character app
 * ID and key from your Pulsate admin. Keep this template blank, and never
 * commit real keys.
 */
export const LOCAL_APP_ID: string = '';
export const LOCAL_APP_KEY: string = '';
